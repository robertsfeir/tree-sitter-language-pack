// This file is auto-generated by alef. DO NOT EDIT.
// alef:hash:9089d7d6e9db22a4ddcf8b62702e52a57df194660d826d73901e482b5c7efda8
#nullable enable

using System;
using System.Collections.Generic;
using System.Runtime.InteropServices;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace TreeSitterLanguagePack;

public static class TreeSitterLanguagePackConverter
{
    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault
    };

    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull
    };

    /// <summary>
    /// List all available language names (sorted, deduplicated, includes aliases).
    ///
    /// Returns names of both statically compiled and dynamically loadable languages,
    /// plus any configured aliases.
    /// </summary>
    public static List<string> AvailableLanguages()
    {
        var nativeResult = NativeMethods.AvailableLanguages();
        if (NativeMethods.LastErrorCode() != 0)
        {
            throw GetLastError();
        }
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        var returnValue = JsonSerializer.Deserialize<List<string>>(json ?? "null", JsonOptions)!;
        return returnValue;
    }

    /// <summary>
    /// Return the effective cache directory path.
    ///
    /// This is `{base}/tree-sitter-language-pack/v{version}/libs/`, where `{base}` is
    /// either the custom BASE directory set via `configure` / `init`
    /// (`PackConfig.cache_dir`) or the platform default cache directory — both are
    /// suffixed identically, so a custom `cache_dir` is never used as the final libs
    /// path. The default resolves to `~/.cache/tree-sitter-language-pack/v{version}/libs/`
    /// on a typical Unix system.
    /// </summary>
    /// <exception cref="TreeSitterLanguagePackException">Returns an error if no cache directory can be resolved: `version` is somehow
    /// invalid, or (with no custom `cache_dir` configured) the platform reports none
    /// and `TREE_SITTER_LANGUAGE_PACK_CACHE_DIR` is unset. This crate no longer falls
    /// back to the temporary directory for the latter case — see #101 H2.</exception>
    public static string CacheDir()
    {
        var nativeResult = NativeMethods.CacheDir();
        if (NativeMethods.LastErrorCode() != 0)
        {
            throw GetLastError();
        }
        var returnValue = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(nativeResult) ?? string.Empty;
        NativeMethods.FreeString(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Delete all cached parser shared libraries.
    ///
    /// Resets the cache registration so the next call to `get_language` or
    /// a download function will re-register the (now empty) cache directory.
    /// </summary>
    /// <exception cref="TreeSitterLanguagePackException">Returns an error if the cache directory cannot be removed.</exception>
    public static void CleanCache()
    {
        var nativeResult = NativeMethods.CleanCache();
        if (nativeResult != 0)
        {
            throw GetLastError();
        }
    }

    /// <summary>
    /// Apply download configuration without downloading anything.
    ///
    /// Use this to set a custom cache directory before the first call to
    /// `get_language` or any download function. Changing the cache dir
    /// after languages have been registered has no effect on already-loaded
    /// languages.
    ///
    /// `PackConfig.cache_dir` is a BASE directory, not the final libs path: this
    /// crate appends `tree-sitter-language-pack/v{version}/libs` to it, the same
    /// suffix applied to the platform default cache directory. In the example below,
    /// files actually land under `/tmp/my-parsers/tree-sitter-language-pack/v{version}/libs/`,
    /// never directly in `/tmp/my-parsers/`. Call `cache_dir` to read back the
    /// resolved, fully-suffixed path.
    /// </summary>
    /// <exception cref="TreeSitterLanguagePackException">Returns an error if the lock cannot be acquired.</exception>
    /// <param name="config"></param>
    public static void Configure(PackConfig config)
    {
        ArgumentNullException.ThrowIfNull(config);
        var configJson = JsonSerializer.Serialize(config, JsonSerializationOptions);
        var configHandle = NativeMethods.PackConfigFromJson(configJson);
        if (configHandle == 0)
        {
            throw TreeSitterLanguagePackException.FromLastError("PackConfigFromJson failed");
        }
        try
        {
            var nativeResult = NativeMethods.Configure(
                configHandle
            );
            if (nativeResult != 0)
            {
                throw GetLastError();
            }
        }
        finally
        {
            if (configHandle != 0) NativeMethods.PackConfigFree(configHandle);
        }
    }

    /// <summary>
    /// Detect language name from a file path or extension.
    ///
    /// This compatibility alias matches the pre-Alef Python binding API.
    /// </summary>
    /// <param name="path"></param>
    public static string? DetectLanguage(string path)
    {
        ArgumentNullException.ThrowIfNull(path);
        var nativeResult = NativeMethods.DetectLanguage(
            path
        );
        if (nativeResult == IntPtr.Zero)
        {
            return null;
        }
        var returnValue = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Detect language name from file content using the shebang line (`#!`).
    ///
    /// Inspects only the first line of `content`. If it begins with `#!`, the
    /// interpreter name is extracted and mapped to a language name.
    ///
    /// Handles common patterns:
    /// - `#!/usr/bin/env python3` → `"python"`
    /// - `#!/bin/bash` → `"bash"`
    /// - `#!/usr/bin/env node` → `"javascript"`
    ///
    /// The `-S` flag accepted by some `env` implementations is skipped automatically.
    /// Version suffixes (e.g. `python3.11`, `ruby3.2`) are stripped before matching.
    ///
    /// A leading UTF-8 BOM (`U+FEFF`) is skipped before the `#!` check, so a
    /// BOM-prefixed script is still detected by its shebang.
    ///
    /// Returns `None` when content does not start with `#!` (after stripping a
    /// leading BOM), the shebang is malformed, or the interpreter is not recognised.
    /// </summary>
    /// <param name="content"></param>
    public static string? DetectLanguageFromContent(string content)
    {
        ArgumentNullException.ThrowIfNull(content);
        var nativeResult = NativeMethods.DetectLanguageFromContent(
            content
        );
        if (nativeResult == IntPtr.Zero)
        {
            return null;
        }
        var returnValue = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Detect language name from a file extension (without leading dot).
    ///
    /// Returns `None` for unrecognized extensions. The match is case-insensitive.
    /// </summary>
    /// <param name="ext"></param>
    public static string? DetectLanguageFromExtension(string ext)
    {
        ArgumentNullException.ThrowIfNull(ext);
        var nativeResult = NativeMethods.DetectLanguageFromExtension(
            ext
        );
        if (nativeResult == IntPtr.Zero)
        {
            return null;
        }
        var returnValue = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Detect language name from a file path.
    ///
    /// Extracts the file extension and looks it up. Returns `None` if the
    /// path has no extension or the extension is not recognized.
    /// </summary>
    /// <param name="path"></param>
    public static string? DetectLanguageFromPath(string path)
    {
        ArgumentNullException.ThrowIfNull(path);
        var nativeResult = NativeMethods.DetectLanguageFromPath(
            path
        );
        if (nativeResult == IntPtr.Zero)
        {
            return null;
        }
        var returnValue = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Download specific languages to the local cache.
    ///
    /// Returns the number of distinct languages available after the call. Already
    /// compiled or cached languages are included in the count.
    ///
    /// Aliases are resolved before counting, so `["shell", "bash"]` names one
    /// language and returns 1.
    /// </summary>
    /// <exception cref="TreeSitterLanguagePackException">Returns an error if any language is not available in the manifest or if
    /// the download fails.</exception>
    /// <param name="names"></param>
    public static ulong Download(List<string> names)
    {
        var namesJson = JsonSerializer.Serialize(names, JsonSerializationOptions);
        var namesHandle = global::System.Runtime.InteropServices.Marshal.StringToCoTaskMemUTF8(namesJson);
        try
        {
            var nativeResult = NativeMethods.Download(
                namesHandle
            );
            if (NativeMethods.LastErrorCode() != 0)
            {
                throw GetLastError();
            }
            var returnValue = nativeResult;
            return returnValue;
        }
        finally
        {
            if (namesHandle != global::System.IntPtr.Zero) global::System.Runtime.InteropServices.Marshal.FreeCoTaskMem(namesHandle);
        }
    }

    /// <summary>
    /// Download all available languages from the remote manifest.
    ///
    /// Downloads the platform bundle and extracts every library it contains.
    /// Languages that appear in the manifest but are absent from the bundle
    /// (e.g. grammars that failed to compile at release time) are silently
    /// skipped — they are not treated as an error.
    ///
    /// Returns the total number of languages now available (statically compiled
    /// plus downloaded and cached).
    /// </summary>
    /// <exception cref="TreeSitterLanguagePackException">Returns an error if the manifest cannot be fetched or the bundle download fails.</exception>
    public static ulong DownloadAll()
    {
        var nativeResult = NativeMethods.DownloadAll();
        if (NativeMethods.LastErrorCode() != 0)
        {
            throw GetLastError();
        }
        var returnValue = nativeResult;
        return returnValue;
    }

    /// <summary>
    /// Download every language in a named group.
    ///
    /// Groups are defined by the remote manifest, not by this crate, and let you
    /// ensure a curated set of related grammars in one call instead of listing each
    /// name to `download()`. Already-cached languages are skipped.
    ///
    /// Call `manifest_groups` to discover the group names the manifest actually
    /// defines. The published manifest currently defines a single group, `"all"`;
    /// earlier revisions of this documentation advertised `"web"`, `"data"`, and
    /// `"systems"`, which the manifest has never contained, so every call following
    /// that example failed. Do not hardcode a group name without checking.
    ///
    /// Returns the total number of languages now available (statically compiled
    /// plus downloaded and cached).
    /// </summary>
    /// <exception cref="TreeSitterLanguagePackException">Returns `Error.Download` if the manifest cannot be fetched, if the group
    /// is unknown — the message lists the groups the manifest defines — or if any
    /// constituent language fails to download.</exception>
    /// <param name="name"></param>
    public static ulong DownloadGroup(string name)
    {
        ArgumentNullException.ThrowIfNull(name);
        var nativeResult = NativeMethods.DownloadGroup(
            name
        );
        if (NativeMethods.LastErrorCode() != 0)
        {
            throw GetLastError();
        }
        var returnValue = nativeResult;
        return returnValue;
    }

    /// <summary>
    /// Return languages that are already downloaded and cached locally.
    ///
    /// Does not perform any network requests. Returns an empty list if the
    /// cache directory does not exist or cannot be read.
    /// </summary>
    public static List<string> DownloadedLanguages()
    {
        var nativeResult = NativeMethods.DownloadedLanguages();
        if (NativeMethods.LastErrorCode() != 0)
        {
            throw GetLastError();
        }
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        var returnValue = JsonSerializer.Deserialize<List<string>>(json ?? "null", JsonOptions)!;
        return returnValue;
    }

    /// <summary>
    /// Get the folds query for a language, if bundled.
    ///
    /// Returns the contents of `folds.scm` (used for code folding) as a static string,
    /// or `None` if no folds query is bundled for this language.
    /// </summary>
    /// <param name="language"></param>
    public static string? GetFoldsQuery(string language)
    {
        ArgumentNullException.ThrowIfNull(language);
        var nativeResult = NativeMethods.GetFoldsQuery(
            language
        );
        if (nativeResult == IntPtr.Zero)
        {
            return null;
        }
        var returnValue = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Get the highlights query for a language, if bundled.
    ///
    /// Returns the contents of `highlights.scm` as a static string, or `None`
    /// if no highlights query is bundled for this language.
    /// </summary>
    /// <param name="language"></param>
    public static string? GetHighlightsQuery(string language)
    {
        ArgumentNullException.ThrowIfNull(language);
        var nativeResult = NativeMethods.GetHighlightsQuery(
            language
        );
        if (nativeResult == IntPtr.Zero)
        {
            return null;
        }
        var returnValue = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Get the indents query for a language, if bundled.
    ///
    /// Returns the contents of `indents.scm` (used for auto-indentation) as a static
    /// string, or `None` if no indents query is bundled for this language.
    /// </summary>
    /// <param name="language"></param>
    public static string? GetIndentsQuery(string language)
    {
        ArgumentNullException.ThrowIfNull(language);
        var nativeResult = NativeMethods.GetIndentsQuery(
            language
        );
        if (nativeResult == IntPtr.Zero)
        {
            return null;
        }
        var returnValue = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Get the injections query for a language, if bundled.
    ///
    /// Returns the contents of `injections.scm` as a static string, or `None`
    /// if no injections query is bundled for this language.
    /// </summary>
    /// <param name="language"></param>
    public static string? GetInjectionsQuery(string language)
    {
        ArgumentNullException.ThrowIfNull(language);
        var nativeResult = NativeMethods.GetInjectionsQuery(
            language
        );
        if (nativeResult == IntPtr.Zero)
        {
            return null;
        }
        var returnValue = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        return returnValue;
    }

        /// <summary>
        /// Get a tree-sitter `Language` by name using the global registry.
        ///
        /// Resolves language aliases (e.g., `"shell"` maps to `"bash"`).
        /// When the `download` feature is enabled (default), automatically downloads
        /// the parser from GitHub releases if not found locally.
        /// </summary>
        /// <exception cref="TreeSitterLanguagePackException">Returns `Error.LanguageNotFound` if the language is not recognized,
        /// or `Error.Download` if auto-download fails.</exception>
        public static TreeSitter.Language GetLanguage(string name)
        {
            var nativeResult = NativeMethods.GetLanguage(name);
            if (nativeResult == IntPtr.Zero)
            {
                throw GetLastError();
            }
            if (NativeMethods.LastErrorCode() != 0)
            {
                throw GetLastError();
            }
            return new TreeSitter.Language(nativeResult);
        }
    /// <summary>
    /// Get the locals query for a language, if bundled.
    ///
    /// Returns the contents of `locals.scm` as a static string, or `None`
    /// if no locals query is bundled for this language.
    /// </summary>
    /// <param name="language"></param>
    public static string? GetLocalsQuery(string language)
    {
        ArgumentNullException.ThrowIfNull(language);
        var nativeResult = NativeMethods.GetLocalsQuery(
            language
        );
        if (nativeResult == IntPtr.Zero)
        {
            return null;
        }
        var returnValue = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Get a `Parser` pre-configured for the given language.
    ///
    /// This is a convenience function that calls `get_language` and configures
    /// a new parser in one step.
    /// </summary>
    /// <exception cref="TreeSitterLanguagePackException">Returns `Error.LanguageNotFound` if the language is not recognized, or
    /// `Error.ParserSetup` if the language cannot be applied to the parser.</exception>
    /// <param name="name"></param>
    public static Parser GetParser(string name)
    {
        ArgumentNullException.ThrowIfNull(name);
        var nativeResult = NativeMethods.GetParser(
            name
        );
        if (NativeMethods.LastErrorCode() != 0)
        {
            throw GetLastError();
        }
        var returnValue = new Parser(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Get the tags query for a language, if bundled.
    ///
    /// Returns the contents of `tags.scm` as a static string, or `None`
    /// if no tags query is bundled for this language.
    /// </summary>
    /// <param name="language"></param>
    public static string? GetTagsQuery(string language)
    {
        ArgumentNullException.ThrowIfNull(language);
        var nativeResult = NativeMethods.GetTagsQuery(
            language
        );
        if (nativeResult == IntPtr.Zero)
        {
            return null;
        }
        var returnValue = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Check if a language is available by name or alias.
    ///
    /// Returns `true` if the language can be loaded (statically compiled,
    /// dynamically available, or a known alias for one of these).
    /// </summary>
    /// <param name="name"></param>
    public static bool HasLanguage(string name)
    {
        ArgumentNullException.ThrowIfNull(name);
        var nativeResult = NativeMethods.HasLanguage(
            name
        );
        var returnValue = nativeResult != 0;
        return returnValue;
    }

    /// <summary>
    /// Initialize the language pack with the given configuration.
    ///
    /// Applies any custom cache directory, then downloads all languages and groups
    /// specified in the config. This is the recommended entry point when you want
    /// to pre-warm the cache before use.
    /// </summary>
    /// <exception cref="TreeSitterLanguagePackException">Returns an error if configuration cannot be applied or if downloads fail.</exception>
    /// <param name="config"></param>
    public static void Init(PackConfig config)
    {
        ArgumentNullException.ThrowIfNull(config);
        var configJson = JsonSerializer.Serialize(config, JsonSerializationOptions);
        var configHandle = NativeMethods.PackConfigFromJson(configJson);
        if (configHandle == 0)
        {
            throw TreeSitterLanguagePackException.FromLastError("PackConfigFromJson failed");
        }
        try
        {
            var nativeResult = NativeMethods.Init(
                configHandle
            );
            if (nativeResult != 0)
            {
                throw GetLastError();
            }
        }
        finally
        {
            if (configHandle != 0) NativeMethods.PackConfigFree(configHandle);
        }
    }

    /// <summary>
    /// Return the number of available languages.
    ///
    /// Includes statically compiled languages, dynamically loadable languages,
    /// and aliases.
    /// </summary>
    public static ulong LanguageCount()
    {
        var nativeResult = NativeMethods.LanguageCount();
        var returnValue = nativeResult;
        return returnValue;
    }

    /// <summary>
    /// Return the names of every language group the remote manifest defines, sorted.
    ///
    /// Group names are manifest data, not a compile-time constant of this crate, so
    /// this is the only reliable way to learn what `download_group` and
    /// `PackConfig.groups` accept. The published manifest currently defines just
    /// `"all"`.
    /// </summary>
    /// <exception cref="TreeSitterLanguagePackException">Returns `Error.Download` if the manifest cannot be fetched.</exception>
    public static List<string> ManifestGroups()
    {
        var nativeResult = NativeMethods.ManifestGroups();
        if (NativeMethods.LastErrorCode() != 0)
        {
            throw GetLastError();
        }
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        var returnValue = JsonSerializer.Deserialize<List<string>>(json ?? "null", JsonOptions)!;
        return returnValue;
    }

    /// <summary>
    /// Return all language names available in the remote manifest (371).
    ///
    /// Fetches (and caches) the remote manifest to discover the full list of
    /// downloadable languages. Use `downloaded_languages` to list what is
    /// already cached locally.
    /// </summary>
    /// <exception cref="TreeSitterLanguagePackException">Returns an error if the manifest cannot be fetched.</exception>
    public static List<string> ManifestLanguages()
    {
        var nativeResult = NativeMethods.ManifestLanguages();
        if (NativeMethods.LastErrorCode() != 0)
        {
            throw GetLastError();
        }
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        var returnValue = JsonSerializer.Deserialize<List<string>>(json ?? "null", JsonOptions)!;
        return returnValue;
    }

    /// <summary>
    /// Prefetch grammars: download any not already loadable from disk, then load every
    /// requested language into the process registry so a subsequent hot loop only parses.
    ///
    /// Unlike `download()`, this does not trust in-memory availability — it downloads
    /// whenever a grammar is not actually loadable from disk (fixing the case where a
    /// known-but-not-downloaded grammar is reported present), then resolves and caches
    /// every requested language. Call it once, up front, before a parallel workload.
    /// </summary>
    /// <exception cref="TreeSitterLanguagePackException">Returns `Error.Download` if a required grammar cannot be fetched, or
    /// `Error.LanguageNotFound` if a requested name is unknown.</exception>
    /// <param name="languages"></param>
    public static void Prefetch(List<string> languages)
    {
        var languagesJson = JsonSerializer.Serialize(languages, JsonSerializationOptions);
        var languagesHandle = global::System.Runtime.InteropServices.Marshal.StringToCoTaskMemUTF8(languagesJson);
        try
        {
            var nativeResult = NativeMethods.Prefetch(
                languagesHandle
            );
            if (nativeResult != 0)
            {
                throw GetLastError();
            }
        }
        finally
        {
            if (languagesHandle != global::System.IntPtr.Zero) global::System.Runtime.InteropServices.Marshal.FreeCoTaskMem(languagesHandle);
        }
    }

    /// <summary>
    /// Process source code and extract file intelligence using the global registry.
    ///
    /// Parses the source with tree-sitter and extracts metrics, structure, imports,
    /// exports, comments, docstrings, symbols, diagnostics, and/or chunks based on
    /// the flags set in `ProcessConfig`.
    /// </summary>
    /// <exception cref="TreeSitterLanguagePackException">Returns `Error.InvalidRange` if the config carries a zero-valued limit or
    /// the source exceeds `ProcessConfig.max_source_bytes`,
    /// `Error.ParseTimeout` if the parse exceeds
    /// `ProcessConfig.parse_timeout_ms`, `Error.LanguageNotFound` if the
    /// language is unknown, or `Error.ParseFailed` if parsing yields no tree.</exception>
    /// <param name="source"></param>
    /// <param name="config"></param>
    public static ProcessResult Process(string source, ProcessConfig config)
    {
        ArgumentNullException.ThrowIfNull(source);
        ArgumentNullException.ThrowIfNull(config);
        var configJson = JsonSerializer.Serialize(config, JsonSerializationOptions);
        var configHandle = NativeMethods.ProcessConfigFromJson(configJson);
        if (configHandle == 0)
        {
            throw TreeSitterLanguagePackException.FromLastError("ProcessConfigFromJson failed");
        }
        try
        {
            var nativeResult = NativeMethods.Process(
                source,
                configHandle
            );
            if (NativeMethods.LastErrorCode() != 0)
            {
                throw GetLastError();
            }
            var jsonPtr = NativeMethods.ProcessResultToJson(nativeResult);
            var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
            NativeMethods.FreeString(jsonPtr);
            NativeMethods.ProcessResultFree(nativeResult);
            var returnValue = JsonSerializer.Deserialize<ProcessResult>(json ?? "null", JsonOptions)!;
            return returnValue;
        }
        finally
        {
            if (configHandle != 0) NativeMethods.ProcessConfigFree(configHandle);
        }
    }

    /// <summary>
    /// Enable chunking with the given maximum chunk size in bytes.
    /// </summary>
    /// <param name="maxSize"></param>
    public static ProcessConfig ProcessConfigWithChunking(ulong handle, ulong maxSize)
    {
        var nativeResult = NativeMethods.ProcessConfigWithChunking(
            handle,
            maxSize
        );
        if (nativeResult == 0)
        {
            throw GetLastError();
        }
        var jsonPtr = NativeMethods.ProcessConfigToJson(nativeResult);
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.ProcessConfigFree(nativeResult);
        var returnValue = JsonSerializer.Deserialize<ProcessConfig>(json ?? "null", JsonOptions)!;
        return returnValue;
    }

    /// <summary>
    /// Enable every analysis feature, including data extraction.
    ///
    /// Chunking is not an analysis feature and stays off; enable it with
    /// `with_chunking`.
    /// </summary>
    public static ProcessConfig ProcessConfigAll(ulong handle)
    {
        var nativeResult = NativeMethods.ProcessConfigAll(
            handle
        );
        if (nativeResult == 0)
        {
            throw GetLastError();
        }
        var jsonPtr = NativeMethods.ProcessConfigToJson(nativeResult);
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.ProcessConfigFree(nativeResult);
        var returnValue = JsonSerializer.Deserialize<ProcessConfig>(json ?? "null", JsonOptions)!;
        return returnValue;
    }

    /// <summary>
    /// Disable all analysis features (only metrics computed).
    /// </summary>
    public static ProcessConfig ProcessConfigMinimal(ulong handle)
    {
        var nativeResult = NativeMethods.ProcessConfigMinimal(
            handle
        );
        if (nativeResult == 0)
        {
            throw GetLastError();
        }
        var jsonPtr = NativeMethods.ProcessConfigToJson(nativeResult);
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.ProcessConfigFree(nativeResult);
        var returnValue = JsonSerializer.Deserialize<ProcessConfig>(json ?? "null", JsonOptions)!;
        return returnValue;
    }

    /// <summary>
    /// Enable or disable hierarchical data extraction for data-format files.
    ///
    /// When `true`, `ProcessResult.data` is
    /// populated with a key/value tree for supported data-format languages.
    /// </summary>
    /// <param name="enabled"></param>
    public static ProcessConfig ProcessConfigWithDataExtraction(ulong handle, bool enabled)
    {
        var nativeResult = NativeMethods.ProcessConfigWithDataExtraction(
            handle,
            (enabled ? 1 : 0)
        );
        if (nativeResult == 0)
        {
            throw GetLastError();
        }
        var jsonPtr = NativeMethods.ProcessConfigToJson(nativeResult);
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.ProcessConfigFree(nativeResult);
        var returnValue = JsonSerializer.Deserialize<ProcessConfig>(json ?? "null", JsonOptions)!;
        return returnValue;
    }

    /// <summary>
    /// Reject source longer than `max_bytes` instead of parsing it.
    ///
    /// Pass `None` to restore the default unbounded behaviour. See
    /// `RECOMMENDED_MAX_SOURCE_BYTES` for a starting value.
    /// </summary>
    /// <param name="maxBytes">Optional.</param>
    public static ProcessConfig ProcessConfigWithMaxSourceBytes(ulong handle, ulong? maxBytes)
    {
        var nativeResult = NativeMethods.ProcessConfigWithMaxSourceBytes(
            handle,
            maxBytes ?? ulong.MaxValue
        );
        if (nativeResult == 0)
        {
            throw GetLastError();
        }
        var jsonPtr = NativeMethods.ProcessConfigToJson(nativeResult);
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.ProcessConfigFree(nativeResult);
        var returnValue = JsonSerializer.Deserialize<ProcessConfig>(json ?? "null", JsonOptions)!;
        return returnValue;
    }

    /// <summary>
    /// Cancel the parse if it exceeds `timeout_ms` milliseconds of wall clock.
    ///
    /// Pass `None` to restore the default (no timeout). See
    /// `RECOMMENDED_PARSE_TIMEOUT_MS` for a starting value.
    /// </summary>
    /// <param name="timeoutMs">Optional.</param>
    public static ProcessConfig ProcessConfigWithParseTimeoutMs(ulong handle, ulong? timeoutMs)
    {
        var nativeResult = NativeMethods.ProcessConfigWithParseTimeoutMs(
            handle,
            timeoutMs ?? ulong.MaxValue
        );
        if (nativeResult == 0)
        {
            throw GetLastError();
        }
        var jsonPtr = NativeMethods.ProcessConfigToJson(nativeResult);
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.ProcessConfigFree(nativeResult);
        var returnValue = JsonSerializer.Deserialize<ProcessConfig>(json ?? "null", JsonOptions)!;
        return returnValue;
    }

    /// <summary>
    /// Check that the configured limits are usable before they drive a parse.
    ///
    /// Called by `process` and
    /// `LanguageRegistry.process`; call it
    /// directly to reject a bad configuration early.
    /// </summary>
    /// <exception cref="TreeSitterLanguagePackException">Returns `Error.InvalidRange` when
    /// `chunk_max_size`, `max_source_bytes`, or `parse_timeout_ms` is `Some(0)`.
    /// Zero is always a configuration mistake: `None` is how each of these is
    /// disabled, so `Some(0)` could only mean "produce nothing".</exception>
    public static void ProcessConfigValidate(ulong handle)
    {
        var nativeResult = NativeMethods.ProcessConfigValidate(
            handle
        );
        if (nativeResult != 0)
        {
            throw GetLastError();
        }
    }

    private static Exception GetLastError()
    {
        return TreeSitterLanguagePackException.FromLastError("Unknown error");
    }
}
